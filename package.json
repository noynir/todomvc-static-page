{
  "name": "todomvc-static-page",
  "version": "1.0.0",
  "description": "todoMVC static page",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --port 3000 --files index.html --files css/main.css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "todoMVC",
    "static",
    "page"
  ],
  "author": "Liucw",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.8.2"
  }
}
